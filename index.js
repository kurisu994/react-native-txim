import ImMsg from './im/Message'
import ImInit from './im/ImInit'
/**
 * listenerRecentContact //'最近会话'
 * listenerOnlineStatus //'在线状态'
 * listenerFriend //'联系人'
 * listenerTeam //'群组'
 * listenerReceiveMessage//'接收消息'
 *
 * listenerDeleteMessage //'撤销后删除消息'
 * listenerReceiveSystemMsg //'系统通知'
 * listenerMsgStatus //'发送消息状态变化'
 * listenerAudioRecord //'录音状态'
 * listenerUnreadCountChange //'未读数变化'
 * listenerBlackList //'黑名单'
 * listenerAttachmentProgress//'上传下载进度'
 * listenerOnKick //'被踢出'
 * listenerAccountNotice //'账户变动通知'
 * listenerLaunchPushEvent
 * listenerBackgroundPushEvent
 *
 */

export{
    ImMsg,
    ImInit
}
